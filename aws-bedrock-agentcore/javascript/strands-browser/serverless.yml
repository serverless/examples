# Strands Browser Example
# Deploy: serverless deploy
# Test: RUNTIME_ARN=<arn> node test-invoke.js

service: strands-browser

frameworkVersion: '4'

provider:
  name: aws
  region: us-east-1

ai:
  agents:
    browserAgent: {}
