# LangGraph JS Minimal Example
#
# A minimal LangGraph JavaScript agent with built-in tools.
# Docker is auto-detected from Dockerfile in current directory.

service: langgraph-basic-dockerfile

frameworkVersion: '4'

provider:
  name: aws
  region: us-east-1

ai:
  agents:
    # Runtime agent - Docker auto-detected, no extra config needed
    assistant: {}
