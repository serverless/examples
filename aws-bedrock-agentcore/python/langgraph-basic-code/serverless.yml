service: langgraph-basic-code

frameworkVersion: '4'

provider:
  name: aws
  region: us-east-1

ai:
  agents:
    # Minimal LangGraph agent with built-in tools
    # Code deployment (uses Docker only for dependency packaging)
    chatbot:
      handler: agent.py
