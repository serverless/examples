# Strands Browser Example
# Deploy: serverless deploy
# Test: python test-invoke.py

service: strands-browser

frameworkVersion: '4'

provider:
  name: aws
  region: us-east-1

ai:
  agents:
    browserAgent: {}
